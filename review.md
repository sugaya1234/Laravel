# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
todosテーブルから全てのレコードを取得する

### Todoモデルのallメソッドの返り値は何か
Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
配列と違い、要素の追加や削除が自由に行える他、Laravelで用意されているメソッドでデータ操作を効率化できる。

### view関数の第1・第2引数の指定と何をしているか
view関数はControllerから、HTMLを表すbladeファイルへデータを渡している
第一引数は画面に表示したいbladeを、
第二引数は渡したいデータの連想配列を指定している

### index.blade.phpの$todos・$todoに代入されているものは何か
$todosにはtodosテーブルから取得したすべてのレコードが
$todoにはクラスのインスタンスが代入されている

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
フォームから入力された入力値を、キーが入力欄のname属性、valueが入力値とする連想配列として一括で取得している

### fillメソッドは何をしているか
Todoインスタンスの複数のプロパティに一括でデータを代入している

### $fillableは何のために設定しているか
fill()がModelに代入できるプロパティを指定している

### saveメソッドで実行しているSQLは何か
オブジェクトの状態をDBに保存する

### redirect()->route()は何をしているか
route()の引数に指定されたルートにリダイレクトする

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくても、PHPコードでテーブル操作ができる
マイグレーションファイルをGitで共有することで、マイグレーションを実行するだけで、開発者全員が同じテーブルを作成することができる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()が実行されるのは、artisanコマンドを実行したとき
down()が実行されるのは、rollbackコマンドを実行したとき

### Seederクラスの役割は何か
データベースにテストデータを登録するために使用する

### route関数の引数・返り値・使用するメリット
引数はルート名で、そのルートの名前付きルートを定義する
返り値はそのルートで設定したURL
URLの記述が簡潔になり可読性が向上し、URLに変更がある場合でも、ルート名に変更がなければ、web.phpを修正するだけで済む

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extendsで承継する親bladeを指定
子bladeの@section～@endsectionで囲われた部分に、親Bladeの@yieldの部分を挿入している
共通部分を別bladeに分割することで、共通部分に修正が必要な場合に、親bladeの変更するだけで済むようになる

### @csrfは何のための記述か
CSRFトークンを自動的に生成する
トークンを発行することで、他サイトからのリクエストを受け付けないようにし、CSRF攻撃からアプリケーションを保護することができる

### {{ }}とは何の省略系か
PHPのhtmlspecialchars ()
Bladeの{{ }}記法は、自動的にPHPのhtmlspecialchars関数を通されエスケープ処理が行われ、文字列として出力する。