# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
ルート定義で指定したルートパラメータ。
Route::put('/todo/{id}', 'TodoController@update')->name('todo.update');の{id}の部分。

### findメソッドで実行しているSQLは何か
SELECT * FROM todos WHERE id = $id;

### findメソッドで取得できる値は何か
更新対象となるデータのレコード

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
プライマリキーの値が存在するかしないかで切り替えている。
今回の場合は、そのIDを持つレコードがデータベースに存在している場合はUPDATEを、
存在しない場合はINSERTを実行する。

## Todo論理削除

### traitとclassの違いとは
クラス継承では1つのクラスしか継承できないが、traitは1つのクラスに複数のトレイトを追加することができる。
トレイトはクラスと違いインスタンス化できない。

### traitを使用するメリットとは
複数のクラス間でコードを共通化・再利用することができるようになる。
また、クラスに影響を与えず機能を追加することができる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
ルートに対応するコントローラーメソッドが呼び出されたときに、そのメソッドが実行される前のタイミングで実行される。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションルールを適用するため。

### $errorsのhasメソッドの引数・返り値は何か
引数は入力欄のname属性で、返り値はエラーが存在する場合はtrue、存在しない場合はfalse。

### $errorsのfirstメソッドの引数・返り値は何か
引数は入力欄のname属性で、返り値はエラーメッセージの文字列。

### フレームワークとは何か
Webアプリケーションを効率的に開発するための枠組みとなるプログラムの集合。

### MVCはどういったアーキテクチャか
開発効率を高めるため、Model (モデル)、View (ビュー)、Controller (コントローラ)に分けて作られた構造。

### ORMとは何か、またLaravelが使用しているORMは何か
プログラミング言語のClassとデータベースのテーブルをマッピングするツール。
マッピングし、データベースとマッピングされたClassのメソッドを用いることで、
SQLを直接操作することなく、DBとやり取りを行うことができる。
LaravelではORMの一つであるEloquentが用いられている。

### composer.json, composer.lockとは何か
Composerがライブラリやパッケージを管理しているファイル。
Composerは、PHPのパッケージ・ライブラリ管理ツールのこと。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
venderディレクトリ内。
